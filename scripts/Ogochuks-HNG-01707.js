const fullName = "Ogochukwu Odom";
const id = "HNG-01707";
const language = "JavaScript";
const email = "ogochukwuodom@gmail.com"

printString(fullName, id, language, email)

function printString(name, id, lang, email) {
    console.log(`Hello World, this is ${name} with HNGi7 ID ${id} and email ${email} using ${lang} for stage 2 task`);

    return 
}

